# toxic-comment-classification-
Multi-label toxic comment classification using the Jigsaw dataset. Includes text preprocessing, class imbalance handling, a TF–IDF + Logistic Regression baseline, and a BiLSTM deep learning model. Evaluation uses macro F1, per-label precision, recall, and threshold tuning.
